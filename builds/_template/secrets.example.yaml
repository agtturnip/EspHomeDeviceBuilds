# Copy to secrets.yaml in this folder and fill in your local credentials.
# This file is NOT committed; it is loaded by ESPHome at build time.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
api_encryption_key: "YOUR_32_BYTE_BASE64_KEY"
ota_password: "YOUR_OTA_PASSWORD"
